<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.todak">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Include required permissions for Google Mobile Ads to run. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Intro.map"
            android:label="@string/title_activity_map"></activity>
        <!-- Include the AdActivity configChanges and theme. -->
        <activity
            android:name="com.google.android.gms.ads.AdActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"
            android:theme="@android:style/Theme.Translucent" />
        <activity android:name=".Intro.self_diagnosis_lung_result2" />
        <activity android:name=".Intro.self_diagnosis_lung_result" />
        <activity android:name=".Intro.self_diagnosis_lung12" />
        <activity android:name=".Intro.self_diagnosis_lung11" />
        <activity android:name=".Intro.self_diagnosis_lung10" />
        <activity android:name=".Intro.self_diagnosis_lung9" />
        <activity android:name=".Intro.self_diagnosis_lung8" />
        <activity android:name=".Intro.self_diagnosis_lung7" />
        <activity android:name=".Intro.self_diagnosis_lung6" />
        <activity android:name=".Intro.self_diagnosis_lung5" />
        <activity android:name=".Intro.self_diagnosis_lung4" />
        <activity android:name=".Intro.self_diagnosis_lung3" />
        <activity android:name=".Intro.self_diagnosis_lung2" />
        <activity android:name=".Intro.self_diagnosis_lung1" />
        <activity android:name=".Intro.self_diagnosis" />
        <activity android:name=".Intro.Doubt_search" />
        <activity android:name=".Intro.Main2Activity" />
        <activity android:name=".Intro.MainActivity" />
        <activity android:name=".Intro.Disease_search" />
        <activity android:name=".Intro.IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>